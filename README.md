# geoserver-with-empty-layer

# Purpose
This is an instant GeoServer with an empty WMS layer (layername geo:empty).
This recipe was made to play around with embedded feature function in SLD.

`docker-compose up -d`
